<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-3.1.xsd">

    <http pattern="/restful/**" create-session="stateless">
        <intercept-url pattern="/cn/mobiledaily/domain" access="ROLE_REMOTE"/>
        <http-basic/>
    </http>
    <http pattern="/resources/**" security="none"/>
    <http pattern="/login.html*" security="none"/>
    <http auto-config="true" use-expressions="true">
        <form-login login-page="/login.html"/>
        <logout logout-url="/login.html" delete-cookies="JSESSIONID"/>
        <!--<intercept-url pattern="/login.html" access="permitAll()"/>-->
        <intercept-url pattern="/user/**" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/cn/mobiledaily/domain" access="isAuthenticated()"/>

        <session-management>
            <concurrency-control max-sessions="1"/>
        </session-management>
    </http>
    <authentication-manager>
        <authentication-provider user-service-ref="userServiceImpl">
            <password-encoder hash="sha">
                <salt-source user-property="name"/>
            </password-encoder>
        </authentication-provider>
        <authentication-provider>
            <user-service>
                <user name="admin" password="admin" authorities="ROLE_ADMIN,ROLE_USER"/>
                <user name="user" password="user" authorities="ROLE_USER"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>
</beans:beans>